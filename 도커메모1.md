프로세서 안에서 다른 프로세서를 불러내는 것이 api다 같은 프로세서 내에서는 아니다

도커 파일=하나의 이미지 파일을 만드는것, 이 파일을 누가 만들었는지 어떤 업종에서 어떻게 쓸것인가 명시적으로 들어가있는것 
도커compose= 여러개의 이미지를 하나로 묶고 싶을때(프론트,백엔드를 묶을때), 권장하진 않는다, 로그파일정도만 권장한다
권장하지 않는 이유= 묶었는데 로그 서버가 죽었을때 에러메세지가 뜨고 서비스가 안된다 그랬을때 격리를 통해 보호하기 위해서 이다.
(도커는 생각보다 일찍 없어질거 같다..) 

이미지하나하나를 레이어라고 부른다

포트 퍼블리싱= 문이 두개라서..

도커 컴포즈하고 다른 컴포즈 하고 서비스 이름으로 통신한다 ip로 통신 안한다 
ip대신 도메인 네임으로 들어갔는데 이미지에는 도메인 이없다. 그래서 서비스 네임으로 통신한다
vlan이 다른건 서로 통신못한다
컨테이너 묶어놓은걸 하드, 하드끼리는 ip를 가진다 vpc를 가져 서로 통신한다

**도커 데몬(Docker Daemon)**은 도커 엔진의 핵심 백그라운드 프로세스로, 실제로 컨테이너를 생성하고 실행하며 관리하는 "실무자"이자 "엔진" 역할을 합니다.
